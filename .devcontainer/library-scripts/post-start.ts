#!/usr/bin/env -S deno run --allow-all

///////////////////////////////////////////////////////////////////////////////
// WELCOME
///////////////////////////////////////////////////////////////////////////////

import { $ } from "npm:zx";

$.verbose = false;

const SRC = Deno.env.get("SRC");
const HOME = Deno.env.get("HOME");

await $`echo "alias run=${SRC}/run/bin/cmd.ts" >> ${HOME}/.zshrc`;

///////////////////////////////////////////////////////////////////////////////
// END
//////////////////////////////////////////////////////////////////////////////
